<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      Declared as not required so the app installs on a Fire TV Stick, which has no touchscreen,
      and is not refused on a device without the full Leanback stack.
    -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".RetroGuideApp"
        android:allowBackup="false"
        android:banner="@drawable/banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="false"
        android:theme="@style/Theme.RetroGuide">

        <!--
          One activity. The app opens straight into full-screen playback; the guide, settings and
          login are Compose destinations inside it, not separate activities, so the single
          ExoPlayer instance is never torn down by a navigation change.
        -->
        <activity
            android:name=".MainActivity"
            android:banner="@drawable/banner"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.RetroGuide">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- The Leanback entry is what puts the app on the Fire TV home row. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <!-- Kept so the app is also reachable on a phone or tablet for development. -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          WorkManager's default initialiser is removed because the app provides its own
          Configuration from RetroGuideApp.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge"
            xmlns:tools="http://schemas.android.com/tools">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
